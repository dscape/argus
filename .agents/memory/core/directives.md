- Keep repo changes minimal; prefer shrinking plan text over expanding it.
- Do not commit local `.agents/memory/*` changes.
- Preserve explicit invalidates/supersedes links when replacing prior memory.
- In shell calculations, use `python3`, not `python`.
- User prefers very simple, succinct explanations and wants visual artifacts for image geometry.
- Physical transient-label UX: use existing move list as labeling surface; no separate transient panel. Infer capture/check from replay/board state. Move-row selection must not change current frame. Show `!` until both touch markers are set, then `fstart > freplay > fend`; controls `Touch start`/`Touch end`; per-marker `×` resets; hand-occlusion edits happen directly on frame; show `i/o/p` shortcut keycaps; edits autosave.
- Transient autosave must save from current ref-backed draft snapshots on each mutation, preserve later edits during in-flight saves, wait for baseline hydration/sync before saving local edits, keep local-dirty separate from raw draft-vs-baseline diffs, merge occlusion toggles against refreshed baselines, block autosave while baseline-sync draft updates are pending, clear stale blockers, and clear dirty state immediately on save ack. This supersedes older autosave assumptions.
- `refactor/piece_projection` constraints remain: `pipeline/physical/piece_projection.py` is sole physical geometry source; keep `square_manifest.jsonl` writer/live annotation tooling; production stays board-probe + promoted decoder; two-stage stays experimental; keep `autoresearch/` out of README.
- For physical board-reading diagnosis, treat `outputs/piece_projection_review2/` and matching full-frame projected overlays as source-of-truth geometry signature; reject naive grid/full-frame visualizations that do not match active piece-projection contract.
- For `/evaluate/physical`, main inspection image must be original source frame or a board-focused crop derived from it, with client-drawn projected geometry; do not regress to misleading pre-rendered or crop-space-only overlays.
- Autoresearch loop constraints: each turn inspect `autoresearch/results.tsv`, `autoresearch/run.log`, `autoresearch/best_train.py`, and `autoresearch/train.py`; edit only `autoresearch/train.py`; do not run it; do not inspect `autoresearch/overnight.log` or `.pid`; keep changes confined to `autoresearch/` unless decoder context is needed; stay decoder-only; make one small reviewable change; keep runs cheap (<30s target, 120s hard cap); caps remain `beam_size<=8`, `top_move_candidates<=16`, `top_board_candidates<=4`, `max_event_proposals<=24`, `event_window_radius<=2`, `state_aware_proposal_passes<=1`, `min_event_separation>=2`; avoid `state_aware_proposal_passes=1` because it caused runaway runtime.
- Autoresearch best-run bookkeeping compares against exact `autoresearch/best_result.json`, not rounded `results.tsv` rows or same-config auto-keep logic. This supersedes older keep/discard assumptions tied to TSV rounding.
- In current machine-precision autoresearch regime, recompute midpoints from the true executed harmful/flat bracket with high-precision Decimal math (`getcontext().prec = 60` or higher) after inspection; avoid stale midpoint reuse or duplicate probes.
- Current machine-precision autoresearch experiments must keep `EXPERIMENT_NAME` shortened (`hybrid_p8_m8_w3_v###` style) to avoid `write_report_json` path-length failures. This supersedes older long-name assumptions.
