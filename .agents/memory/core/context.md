- Dev-tools now includes Evaluate -> Failures viewer at `/evaluate/failures`, backed by `/api/evaluate/physical-failures/*`.
- Viewer/service auto-discover failure-study bundles under `outputs/`, load `summary.json` + `manifest.json` + `manual_buckets.csv`, show baseline metrics/bucket counts, scrub leading raw frames for a selected failure, render crop + GT/stateless/decoded chess boards with the same failure-study color semantics, show legal/square diagnostics, and save `final_bucket`/`notes` back to `manual_buckets.csv`.
- Failure-study pipeline was updated from frame sampling to episode sampling: first failing frame plus preceding context, capped/round-robin by `source_video_id`; summary fields now use `total_episodes`/`selected_episodes`, with `preceding_frames`, `recovery_gap`, and `max_per_video` semantics.
- Bucket labels were standardized to user-facing slash names: `rectification / localization`, `piece classifier / square evidence`, `temporal in-between / move execution ambiguity`, `decoder / wrong legal hypothesis / error propagation`, `eval or label issue`, `other / unclear`.
- Dev-tools/service/frontend were adjusted to episode-based manifests and CSV persistence; old flat manifests still have fallback pseudo-episode support.
- `outputs/plan.md` was updated: Step 1 is now recurring diagnosis each loop, and the viewer/tooling already exists.
- Regenerated study for baseline `outputs/2026-04-16/tracker_sweep_rectified_realplusmanual/eval_w2_m10.json` at `outputs/2026-04-16/physical_board_failure_study_rectified_realplusmanual_w2_m10/`: `total_episodes=14`, `selected_episodes=13` (cap `max_per_video=5`), with 12 suggested `temporal in-between / move execution ambiguity` and 1 `piece classifier / square evidence`.
- Lesson: on this baseline, dominant failures are around move-execution timing; decoder missing the transition frame can cause long desync episodes. This supersedes older frame-sampled intuition for this run.
- User-facing attachment added: `.agents/memory/attachments/physical_crop_types_example.png`, a 6-panel comparison of crop families for explaining source/native/oblique/rectified/tile views.
