- Failure viewer/service at `/evaluate/failures` uses `/api/evaluate/physical-failures/*`; `docker-compose.yaml` must mount `./outputs:/app/outputs`; recreate container after adding mount.
- Failure-study pipeline is episode-based and viewer auto-discovers bundles under `outputs/`; the 2026-04-16 rectified realplusmanual study (`total_episodes=14`, `selected_episodes=13`) was dominated by temporal ambiguity and supersedes older frame-sampled intuition.
- Physical annotation tooling supports split-local transient labels in `data/physical/{train,val}/transient_annotations.jsonl` via `/api/physical-{eval,train}/transient-annotation` and `/save-transient-annotation`; the existing move list is the labeling UI, hand-occluded frames are toggled on the source frame, capture is inferred from replay state, and edits autosave with serialized writes plus unchanged-payload failure suppression.
- Clip index completion/checkmark for `/annotate/physical` uses transient annotation status rather than board-frame completeness; semantic gap may remain on whether `transient_annotation_complete` exactly matches intended “all moves covered + latent states annotations,” and the user said none should currently show as complete.
- Completed `refactor/piece_projection` reorganisation on branch `refactor/piece_projection`: `pipeline/physical/` is now organized as `board_probe/`, `two_stage/`, and `shared/`; `pipeline/physical/piece_projection.py` is the sole active physical geometry module. This invalidates/supersedes older context saying a separate `authresearch_wholeboard/` workspace still exists or that production runtime still routes through pre-refactor legacy geometry.
- Board-probe runtime/training now use projected square-token sampling; move-model oblique/native-oblique modes were removed instead of ported; `pipeline/physical/board_probe/decoder.py` encodes the production tracker helper.
- Important contract correction after the refactor: board-probe train/eval/diagnostic paths had still been using degraded `224x224` clip tensors from `clip_path` with clip-frame corners, while `outputs/piece_projection_review2/` and two-stage native-crop tooling used source-video frames plus `native_image_bbox`/`native_corners`. `pipeline/physical/board_probe/board_data.py` now preserves native metadata and prefers native source frames when available, with `row.corners` resolved to full-frame native corners; this supersedes the older claim that merely switching from `row.board_path` to `clip_path + frame_index` fully aligned board-probe with the intended piece-projection contract.
- Another remaining contract leak was fixed: `pipeline/physical/shared/move_data.py::load_eval_move_sequences()` now uses `load_annotated_board_rows()` + `load_annotated_board_frame_bgr()`, so annotated move-model eval/selection no longer silently builds piece-projection inputs from 224x224 clip frames plus `corner_space=clip_frame` coordinates.
- Failure-study geometry rendering had also been misleading. This supersedes older context describing a naive grid/full-frame panel: `pipeline/physical/board_probe/failure_study.py` now renders the active piece-projection signature used by `scripts/visualize_piece_projections.py` / `outputs/piece_projection_review2/` (full-frame projected piece boxes + base quads, letterboxed into the episode panel), with GT/predicted change highlights preserved.
- `/evaluate/physical` runtime payload contract changed twice. Earlier server-rendered full-frame overlay imagery was superseded by a raw-source-frame contract: service/runtime visualization now return raw source image data plus source-frame-normalized quads/bboxes for client overlay, while keeping a separate crop thumbnail for session chips. `PhysicalRuntimeInspector` rehydrates legacy sessions with stale crop-space or pre-rendered-image payloads.
- Current `/evaluate/physical` UI behavior: main runtime panel uses the raw source frame as the image source; yellow projections are drawn client-side; hovered square is highlighted in fuchsia on the same image; projected piece-box crop remains the hover inspection preview; session chips keep the board-neighborhood crop thumbnail. User later requested a board-focused crop around the detected board with padding as the displayed runtime view, while preserving overlay alignment.
- Generated diagnostics confirmed the migration changed tokenization, not just overlays: real oblique frames showed piece-box pooling using roughly 6–20 patches per square (mean ~12.05), 124/256 patches shared by multiple squares, and adjacent-square cosine rising from 0.734 (planar) to 0.944 (piece-box); synthetic overlap was much weaker, highlighting a train/eval geometry gap.
- Post-fix retraining/eval comparison on 2026-04-17 showed the corrected piece-projection board-probe path regressed sharply versus its pre-fix promoted numbers (`square 0.6085 -> 0.4705`, `non_empty 0.3233 -> 0.1746`, `macro_f1 0.2886 -> 0.1212`), implying earlier piece-projection metrics were inflated by old rectified-board leakage/mismatch. Two-stage stayed effectively flat (`square ≈0.7683`, `non_empty ≈0.5925`, `board_exact ≈0.00355`).
- After switching board-probe eval to the native source-frame signature, rerunning the promoted checkpoint with production decoder `v282` improved over the clip-frame rerun but still remained well below the stale rectified/autoresearch surface: corrected native eval reached `board_exact=0.05917` vs `0.03550` on the clip-frame rerun and `0.25444` on the stale rectified/autoresearch surface.
- Production physical default is decoder `v282` via `pipeline/physical/board_probe/decoder.py` and `weights/physical/metadata.json`; promoted production tracker eval reached `board_exact_match=0.25443786982248523`, while compatibility eval on the promoted retrained checkpoint preserved the old-lookahead baseline surface at `0.2165680473372781`.
- `autoresearch/prepare.py` now rebuilds `autoresearch/cache/native_realplusmanual_board_logits.pt` from `load_annotated_board_frame_bgr(...)` plus `row.corners`, defaults its fixed baseline to `outputs/2026-04-18/tracker_eval_weights_best_production_native.json`, and verifies by replaying the report’s tracker mode instead of assuming lookahead. Running `.venv/bin/python3 autoresearch/prepare.py --force` reproduced `board_exact=0.05917` on the corrected native surface.
- Verification already completed for the runtime payload + move-data fix: targeted tests plus `make lint`, `make typecheck`, and `make test` passed.
- Relevant dev-tools route reality: `/evaluate/physical` exists and renders; `/annotate/physical` exists; `/annotate/physical-train` appears to redirect/render under `/annotate/physical`; no separate `/annotate/physical-eval` route was found; `/evaluate/physical-runtime` and `/annotate/physical-eval` 404 under current routing.
- `/annotate/physical/...` had an intermittent first-load 500 caused by concurrent rewrites of `data/physical/source_video_splits.json`; `pipeline/physical/shared/splits.py` now writes that manifest atomically and treats a blank file as recoverable empty state.
- Separate screening investigation: apparent `/evaluate/screening` regression was attributed to data/label/domain shift plus brittle non-DINO heuristics (overlay/OTB scores and vertical auto-reject), not to a recent checkpoint or DINO backbone change.
