- Failure viewer/service at `/evaluate/failures` uses `/api/evaluate/physical-failures/*`; `docker-compose.yaml` must mount `./outputs:/app/outputs`; recreate container after adding mount.
- Failure-study pipeline is episode-based; the 2026-04-16 rectified realplusmanual study (`total_episodes=14`, `selected_episodes=13`) was dominated by temporal ambiguity and supersedes older frame-sampled intuition.
- Physical annotation tooling supports split-local transient labels in `data/physical/{train,val}/transient_annotations.jsonl` via `/api/physical-{eval,train}/transient-annotation` and `/save-transient-annotation`; move list is the labeling UI; move-row selection no longer changes frame; hand-occlusion toggles on the source frame; capture inferred from replay state; `i/o/p` shortcuts exist; autosave writes from current draft refs and preserves later edits across in-flight saves.
- Clip-index completion/checkmark for `/annotate/physical` uses transient annotation status rather than board-frame completeness; semantic gap may remain because user said none should currently show complete.
- `refactor/piece_projection` reorg is complete on branch `refactor/piece_projection`: `pipeline/physical/` is organized as `board_probe/`, `two_stage/`, and `shared/`; `pipeline/physical/piece_projection.py` is the sole active physical geometry module. This invalidates older context claiming separate workspaces or pre-refactor production geometry routing.
- Native-frame contract fix supersedes older notes: board-probe train/eval/diagnostic paths preserve native metadata, prefer source-video frames when available, resolve `row.corners` to full-frame native corners, and project full-frame piece boxes before mapping them through the board-neighborhood crop+resize transform for patch pooling. Runtime and pipeline visualizations now use transformed full-frame boxes instead of re-projecting from resized crop corners.
- Verified corrected-native baseline is `outputs/2026-04-18/tracker_eval_weights_best_production_native_pieceboxmapped_off.json` with `board_exact=0.03787`; earlier `...native_pieceboxmapped.json` is superseded for baseline verification.
- `autoresearch/prepare.py` rebuilds corrected-native cache from source-frame board crops plus mapped full-frame projected piece boxes, defaults its fixed baseline to the current piecebox-mapped report, and `--force` reproduces that baseline.
- Diagnostics showed migration changed tokenization, not just overlays: oblique real frames had much heavier square overlap than planar tokenization, exposing a train/eval geometry gap. Post-fix retraining/eval regressed sharply versus pre-fix promoted numbers, implying earlier metrics were inflated by old rectified-board leakage/mismatch. Two-stage remained effectively flat; quick board-probe retrains did not produce a promotable checkpoint.
- Production physical default remains decoder `v282` via `pipeline/physical/board_probe/decoder.py` and `weights/physical/metadata.json`; historical `board_exact≈0.2544` runtime eval is stale/wrong-signature and not trustworthy.
- Two-stage remains experimental after warm-start and MPS retrains; latest MPS eval stayed effectively flat (`board_exact=0.0036`). Whole-board 10-epoch MPS retrain only matched the weak native baseline surface (`board_exact≈0.03787`).
- Autoresearch naming cleanup is done: stale `authresearch` references were renamed to `autoresearch`; controller env vars are `AUTORESEARCH_*`; `autoresearch/controller.sh` points at `autoresearch/train.py`. Older mixed-naming requirements are invalidated.
- In corrected-native autoresearch after `results.tsv` reset, `autoresearch/run.log`, `autoresearch/best_train.py`, and exact `autoresearch/best_result.json` are authoritative.
- Active kept decoder best is `v545`, metrics `0.074556 / 0.792672 / 0.751719 / 0.234375 / 0.009056`. This invalidates older context naming `v501` or `v536` as kept best.
- Decoder frontier summary: `boardcands3` sweet spot; `boardcands2` recovered branch; `boardcands1` harmful; `props14` older optimum; beam `5..7` no-ops, `8` worse; `drop2` harmful; `secsep2/3` worse.
- Recovered branch base is `v609` (`beam3 + lwindow2 + permove137.5`). From it, `v620` (`top_board_candidates=2`) keeps `board_exact=0.074556`, raises recall, and lowers false-change. `LOOKAHEAD_WINDOW=3` opens a higher-recall noisier branch.
- On `lwindow3 + boardcands2`: higher margin initially worsens; radius `1/2` flat; `drop0` harmful. Secondary spacing has two shelves: `secsep1..2` collapse together, `secsep3..6` collapse to a shifted upper shelf.
- Strong side branch from `lwindow3 + boardcands2 + secsep3`: `minsep7` stronger; `minsep8/9` strongest; `minsep10` worse; `v655` strong.
- Newer `secsep1` descendants supersede older uncertainty: `v663` slightly improves `v655`; `v664` exact no-op; `v665` worse; `event_window_radius=1/2`, `secondary_peak_ratio=0.98`, and `segment_board_drop_worst_frames=0` are exact no-ops; `LOOKAHEAD_WINDOW=4` is uniformly bad.
- Distinct `minsep7 + secsep1` margin shelf exists; high shelf persists through `v698` at `9.935836791992188`, low regime starts at `v731`. Cliff-edge high-shelf anchor is `LOOKAHEAD_MARGIN=9.935836791992188`, `beam3`, `top_move_candidates=4`, `top_board_candidates=2`, `min_score_gain_per_added_move=150.0`, `event_window_radius=0`, `secondary_peak_ratio=0.95`, `segment_board_drop_worst_frames=1`; metrics `0.071006 / 0.779288 / 0.743944 / 0.437500 / 0.027167`.
- Narrow-proposal mapping from that anchor found a distinct low-noise shelf: `max_event_proposals=7..8` preserve `board_exact=0.071006`; `props6` breaks sharply. `v758` (`props7`) is strongest low-noise point so far with `0.071006 / 0.799442 / 0.751518 / 0.375000 / 0.021992`. Compact sibling `props8` preserves `board_exact=0.071006` and false-change `0.021992` while raising recall to `0.390625`, but with weaker secondaries; `props9` degrades.
- `props8` local mapping supersedes older pending on `v795`: `top3/top4` equal; `top2/top1` slightly weaker; `top5` worse; `boardcands3/4`, `radius1/2`, `drop0`, and `ratio0.98` are exact no-ops; `secsep2` is worse.
- A distinct lower-noise `props8 + minsep8..9` shelf exists: `v805/v806` anchor at `0.071006 / 0.799339 / 0.749250 / 0.375000 / 0.020699`; `v807` (`minsep10`) breaks sharply. On this shelf: `top3` exact no-op; `boardcands3/4` exact no-ops; `boardcands1` harmful; `beam2` slightly worse; `secsep2` worse; `radius1/2` and `drop0` exact no-ops.
- `v819` (`ratio0.98`) was discarded as an exact no-op; `v820` (`permove137.5`) was harmful at `0.071006 / 0.789778 / 0.739821 / 0.375000 / 0.024580`. This supersedes older prepared-state assumptions for `v819`/`v820`.
- On the `props8 + minsep8` shelf, `min_total_score_gain` is non-binding through `87.5` (`v821..v825` exact no-ops), and `min_score_gain_per_added_move` is flat upward from `150.0` through `300.0` (`v826..v837` exact no-ops).
- Lower-edge binary search on `props8 + minsep8` invalidates older `v865`-waiting context: `v865` was harmful, the harmful/flat bracket tightened through `v883`, and the current executed bracket is `[142.33938090006504, 142.33938090006524]` for `min_score_gain_per_added_move`.
- Current prepared-but-unrun autoresearch edit supersedes older `v865` memory: `v884`, re-encoded from the restored `v545` baseline, sets `min_score_gain_per_added_move=142.33938090006532` on the fixed `props8 + minsep8` anchor. It is likely non-informative because it is already above the last confirmed flat point `v883=142.33938090006524`.
- `autoresearch/run.log` still mainly shows duplicate `AVFFrameReceiver`/`AVFAudioReceiver` dylib warnings rather than decoder failures.
