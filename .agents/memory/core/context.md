- Failure viewer/service at `/evaluate/failures` uses `/api/evaluate/physical-failures/*`; `docker-compose.yaml` must mount `./outputs:/app/outputs`; recreate container after adding mount.
- Failure-study pipeline is episode-based; the 2026-04-16 rectified realplusmanual study (`total_episodes=14`, `selected_episodes=13`) was dominated by temporal ambiguity and supersedes older frame-sampled intuition.
- Physical annotation tooling supports split-local transient labels in `data/physical/{train,val}/transient_annotations.jsonl` via `/api/physical-{eval,train}/transient-annotation` and `/save-transient-annotation`; move list is the labeling UI; move-row selection no longer changes frame; hand occlusion toggles on the source frame; capture inferred from replay state; `i/o/p` shortcuts exist; autosave writes from current draft refs and preserves later edits across in-flight saves.
- Clip-index completion/checkmark for `/annotate/physical` uses transient annotation status rather than board-frame completeness; semantic gap may remain because user said none should currently show complete.
- `refactor/piece_projection` reorg is complete on branch `refactor/piece_projection`: `pipeline/physical/` is organized as `board_probe/`, `two_stage/`, and `shared/`; `pipeline/physical/piece_projection.py` is the sole active physical geometry module. This invalidates older context claiming separate `authresearch_wholeboard/` workspace or pre-refactor production geometry routing.
- Native-frame contract fix supersedes older notes: board-probe train/eval/diagnostic paths preserve native metadata, prefer source-video frames when available, resolve `row.corners` to full-frame native corners, and project full-frame piece boxes before mapping them through the board-neighborhood crop+resize transform for patch pooling. Runtime and pipeline visualizations now use transformed full-frame boxes instead of re-projecting from resized crop corners.
- Verified corrected-native baseline after the full-frame-piecebox-to-crop mapping fix is `outputs/2026-04-18/tracker_eval_weights_best_production_native_pieceboxmapped_off.json` with `board_exact=0.03787`; earlier `...native_pieceboxmapped.json` is superseded for baseline verification.
- `autoresearch/prepare.py` rebuilds `autoresearch/cache/native_realplusmanual_board_logits.pt` from `load_annotated_board_frame_bgr(...)` plus mapped full-frame projected piece boxes, defaults its fixed baseline to the current piecebox-mapped report, and `--force` reproduces that baseline.
- Diagnostics showed migration changed tokenization, not just overlays: oblique real frames had much heavier square overlap than planar tokenization, exposing a train/eval geometry gap. Post-fix retraining/eval regressed sharply versus pre-fix promoted numbers, implying earlier metrics were inflated by old rectified-board leakage/mismatch. Two-stage remained effectively flat; quick board-probe retrains did not produce a promotable checkpoint.
- Production physical default remains decoder `v282` via `pipeline/physical/board_probe/decoder.py` and `weights/physical/metadata.json`; historical `board_exact≈0.2544` runtime eval is stale/wrong-signature and not trustworthy.
- Two-stage remains experimental after warm-start and MPS retrains; latest MPS eval stayed effectively flat (`board_exact=0.0036`). Whole-board 10-epoch MPS retrain only matched the weak native baseline surface (`board_exact≈0.03787`).
- Autoresearch typo cleanup is done: stale `authresearch` references were renamed to `autoresearch`; controller env vars are `AUTORESEARCH_*`; `autoresearch/controller.sh` points directly at `autoresearch/train.py`. Older mixed-naming requirements are invalidated.
- In corrected-native autoresearch after `results.tsv` reset, `autoresearch/run.log`, `autoresearch/best_train.py`, and exact `autoresearch/best_result.json` are authoritative.
- Active kept decoder best is `v545`, metrics `0.074556 / 0.792672 / 0.751719 / 0.234375 / 0.009056`. This invalidates older context naming `v501` or `v536` as kept best.
- Decoder frontier highlights: `boardcands3` sweet spot; `boardcands2` weaker but opens a recovered branch; `boardcands1` harmful; `props14` was an older sharp optimum; beam `5..7` no-ops; beam `8` worse; `drop2` harmful; `secsep2/3` worse.
- Recovered branch base is `v609` (`beam3 + lwindow2 + permove137.5`). From it, `v620` (`top_board_candidates=2`) keeps `board_exact=0.074556`, raises recall, and lowers false-change; `LOOKAHEAD_MARGIN 9.5..9.75` is a plateau, `9.875..10.0` a lower-recall shelf; radius `0..2` flat; `minsep3` sharply worse; `LOOKAHEAD_WINDOW=3` opens a higher-recall noisier branch.
- On `lwindow3 + boardcands2`: higher margin initially worsens; radius `1/2` flat; `drop0` harmful. Secondary spacing has two shelves: `secsep1..2` collapse together, `secsep3..6` collapse to a shifted upper shelf.
- Strong side branch from `lwindow3 + boardcands2 + secsep3`: `minsep7` much stronger; `minsep8/9` strongest; `minsep10` worse; `v655` is strong.
- Newer `secsep1` descendants supersede older uncertainty: `v663` (`secsep1` on stronger `minsep8`) slightly improves `v655`; `v664` exact no-op; `v665` worse; `event_window_radius=1/2` (`v670`,`v671`), `secondary_peak_ratio=0.98` (`v679`), and `segment_board_drop_worst_frames=0` (`v680`) are exact no-ops; `LOOKAHEAD_WINDOW=4` (`v673`-`v678`) is uniformly bad.
- Distinct `minsep7 + secsep1` margin shelf exists: `v681`-`v684` reopen a stronger high-recall shelf; `v686`/`v685` fall to a lower-recall regime; `v669` shows the `minsep7` gain is specific to `secsep1` because `secsep2` reverts to the older noisier surface.
- Binary search localized the `minsep7 + secsep1` cliff: high shelf persists through `v698` at `9.935836791992188`; low regime starts at `v731` (`9.93583679199219`). This supersedes older midpoint-search notes.
- Cliff-edge high-shelf anchor on `lwindow3 + boardcands2 + minsep7 + secsep1` is `LOOKAHEAD_MARGIN=9.935836791992188`, `beam3`, `top_move_candidates=4`, `top_board_candidates=2`, `min_score_gain_per_added_move=150.0`, `event_window_radius=0`, `secondary_peak_ratio=0.95`, `segment_board_drop_worst_frames=1`; anchor metrics are `0.071006 / 0.779288 / 0.743944 / 0.437500 / 0.027167` (`v732/v733/v734/v735/v738/v741`). On this anchor: radius `0..2` flat; `segment_board_drop_worst_frames=0`, `secondary_peak_ratio=0.98`, `min_score_gain_per_added_move=137.5`, and `top_move_candidates=3` are no-ops; `top_move_candidates=1..2`, `secondary_min_event_separation=2`, and `top_board_candidates=3` are worse.
- `v742` (`max_event_proposals=15` on the cliff-edge anchor) ran and was discarded; this invalidates older focus naming `v742` as prepared/unrun.
- Additional cliff-edge mapping: `beam_size=4` non-improving; `top_board_candidates=1` slightly weaker than `boardcands2`; `min_total_score_gain 162.5..187.5` and `min_score_gain_per_added_move 162.5..200.0` are exact-flat/non-binding.
- Narrow-proposal mapping from the cliff-edge anchor found a distinct low-noise shelf: `max_event_proposals=8..7` preserve `board_exact=0.071006` while lowering recall and improving secondaries/false-change; `props6` breaks sharply.
- Current strongest low-noise shelf point is `v758` (`props7`) with `0.071006 / 0.799442 / 0.751518 / 0.375000 / 0.021992`; `top_move_candidates=3` is an exact no-op there, `top1..2` only slightly weaker, `top5..16` exact no-ops, `top_board_candidates=1` harmful, `top_board_candidates=3..4` exact no-ops, `secsep2` worse, `drop0`/`radius1..2`/`ratio0.98`/lower hybrid thresholds exact no-ops, `beam4` exact-flat but ~29.84s, `beam2` slightly weaker, `beam1` much worse, and `minsep7` remains optimal over `6` or `8`.
- Adjacent compact higher-recall sibling: `props8` under the same `beam3 + lwindow3 + boardcands2 + minsep7 + secsep1` family preserves `board_exact=0.071006` and false-change `0.021992` while raising recall to `0.390625`, but with weaker secondaries than `props7`; compact `props9` falls back to the degraded regime.
- `props8` local move-breadth mapping so far: `v792` (`top3`) = `0.071006 / 0.797892 / 0.748744 / 0.390625 / 0.021992`; `v794` (`top2`) keeps board-exact/recall/false-change with slightly weaker secondaries; current `train.py` prepares `v795` (`props8 + top1`) and is awaiting controller execution. This supersedes older focus/pending that treated `props7` as the only active shelf.
