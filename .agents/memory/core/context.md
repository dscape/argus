- Failure viewer/service at `/evaluate/failures` uses `/api/evaluate/physical-failures/*`; `docker-compose.yaml` must mount `./outputs:/app/outputs`; recreate container after adding mount.
- Failure viewer and runtime inspection now show source-frame snapshots with client-drawn piece-projection geometry, projected piece-box crop visualization, raw stateless per-square class probabilities, and a 64-square projected evidence grid when available; this supersedes older inspection context lacking source-frame geometry/probability evidence.
- Failure-study service prefers native source-video frames and native full-frame corners via `pipeline.physical.board_probe.board_data`, not clip-frame-only loading. Default bundle is `outputs/2026-04-19/physical_board_failure_study_current_defaults` for report `outputs/2026-04-19/physical_board_tracker_eval_current_defaults.json`, with `selected_episodes=8`, split `temporal ambiguity: 6`, `piece/square evidence: 2`.
- Physical annotation tooling supports split-local transient labels in `data/physical/{train,val}/transient_annotations.jsonl` via `/api/physical-{eval,train}/transient-annotation` and `/save-transient-annotation`; move list is labeling UI; move-row selection no longer changes frame; hand-occlusion toggles on source frame; capture inferred from replay state; `i/o/p` shortcuts exist; autosave writes from current draft refs and preserves later edits across in-flight saves.
- Clip-index completion/checkmark for `/annotate/physical` uses transient annotation status rather than board-frame completeness; semantic gap may remain because user said none should currently show complete.
- `refactor/piece_projection` reorg is complete on branch `refactor/piece_projection`: `pipeline/physical/` is organized as `board_probe/`, `two_stage/`, and `shared/`; `pipeline/physical/piece_projection.py` is sole active physical geometry module. This invalidates older context claiming separate workspaces or pre-refactor production geometry routing.
- Native-frame contract fix supersedes older notes: board-probe train/eval/diagnostic paths preserve native metadata, prefer source-video frames when available, resolve `row.corners` to full-frame native corners, and project full-frame piece boxes before mapping them through the board-neighborhood crop+resize transform for patch pooling. Runtime and pipeline visualizations now use transformed full-frame boxes instead of re-projecting from resized crop corners.
- Verified corrected-native baseline is `outputs/2026-04-18/tracker_eval_weights_best_production_native_pieceboxmapped_off.json` with `board_exact=0.03787`; earlier `...native_pieceboxmapped.json` is superseded for baseline verification. Current default tracker eval is `outputs/2026-04-19/physical_board_tracker_eval_current_defaults.json` at `board_exact_match=0.05680473372781065` on `845` boards.
- Diagnostics showed migration changed tokenization, not just overlays: oblique real frames had much heavier square overlap than planar tokenization, exposing a train/eval geometry gap. Post-fix retraining/eval regressed sharply versus pre-fix promoted numbers, implying earlier metrics were inflated by old rectified-board leakage/mismatch. Two-stage remained effectively flat; quick board-probe retrains did not produce a promotable checkpoint.
- Production physical default remains decoder `v282` via `pipeline/physical/board_probe/decoder.py` and `weights/physical/metadata.json`; historical `board_exact≈0.2544` runtime eval is stale/wrong-signature and not trustworthy.
- `/evaluate/physical/<session>` runtime inspection uses production legal decoder for displayed decoded board sequence when clip initial state is available, instead of EMA-only smoothed per-frame boards. UI surfaces decoded `stay`/legal-move transition labels and classifies raw stateless changes against previous decoded board as `stay`, `legal_move`, `illegal_single_piece_transfer`, or `unexplained_disturbance`; illegal single-piece transfer labels include promotion suffixes.
- Legacy runtime sessions are auto-refreshed server-side on load when stored results lack geometry/render-mode/transition metadata or raw stateless square probabilities; service re-inspects annotations without inline image payloads, merges fresh decoded/inspection fields while preserving saved session images, and persists upgraded results. Native source-video frame loading is serialized because shared OpenCV `VideoCapture` access caused hangs/socket resets under concurrent dev-tools requests.
- Validation after adding runtime/failure square-evidence inspection: `make test`, `make typecheck`, and `make lint` all passed.
- Corrected-native autoresearch authoritative files are `autoresearch/run.log`, `autoresearch/best_train.py`, and exact `autoresearch/best_result.json`; active kept decoder best is `v545` with metrics `0.074556 / 0.792672 / 0.751719 / 0.234375 / 0.009056`, invalidating older `v501`/`v536` best-memory.
- Decoder frontier condensed: branch base `v609`; `v620` (`top_board_candidates=2`) keeps `board_exact=0.074556` with better recall/lower false-change; `LOOKAHEAD_WINDOW=3` opens a noisier branch. Strong side branch from `lwindow3 + boardcands2 + secsep3`: `minsep8/9` strongest; `v663` slightly improves `v655`; `v664` no-op; `v665` worse; `event_window_radius=1/2`, `secondary_peak_ratio=0.98`, and `segment_board_drop_worst_frames=0` are no-ops; `LOOKAHEAD_WINDOW=4` is bad.
- Lower-noise shelf summary: `props7..8` preserve `board_exact=0.071006`; `props6` breaks sharply. `v758` (`props7`) is strongest low-noise point on secondaries; compact `props8` preserves `board_exact=0.071006` and false-change `0.021992`. On `props8 + minsep8..9`, `v805/v806` anchor at `0.071006 / 0.799339 / 0.749250 / 0.375000 / 0.020699`; `v807` (`minsep10`) breaks sharply. Here `top3`, `boardcands3/4`, `radius1/2`, `drop0`, and `ratio0.98` are no-ops; `boardcands1`, `beam2`, and `secsep2` are worse; `v820` (`permove137.5`) was harmful.
- Machine-precision search supersedes older `v1042`/`v1070`/`v1142-v1141` bracket memory: on compact anchor `props8 + minsep8 + lwindow3 + beam3 + top4 + boardcands2 + secsep1 + ratio0.95 + drop1 + radius0 + total150`, harmful regime stays `0.071006 / 0.789778 / 0.739821 / 0.375000 / 0.024580` and flat regime stays `0.071006 / 0.799339 / 0.749250 / 0.375000 / 0.020699`.
- Current machine-precision bracket supersedes older `[v1329 harmful, v1331 flat]` memory. Authoritative executed search now extends through `v1348`, where `v1348` executed flat with the same flat metrics; duplicate executed names remain possible (`v1332` had both harmful and flat executions), so threshold/metrics and `run.log` outrank suffixes. Supersedes: older context claiming `v1348` was only prepared. Current executed bracket is `[v1347 harmful, v1348 flat]`.
