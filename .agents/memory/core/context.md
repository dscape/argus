- Keep all template-v2 work under `study/templates-v2/`; treat core Argus code as read-only.
- Before coding in this area, read `study/templates/RESULTS.md` and `study/FINAL_DECISION.md`.
- Do not inspect or reuse prior failed files if encountered.
- Stage acceptances require debug visualizations/contact sheets/preview artifacts plus explicit human checkpoints.
- Use explicit `supersedes`/`invalidates` links when replacing prior memory.
- Current active effort is a new template-matching study in `study/templates-v2/`, created after v1 failed on `h2WrtkfwRl8` because starting templates were indistinguishable and DINO embeddings leaked clocks/bottles/adjacent pieces.
- Planned v2 pipeline: geometric visibility gating from FEN + camera pose with per-piece cuboid rasterization and patch-grid visibility masks; foreground isolation; masked patch-token embedding; multi-frame class template aggregation from PGN-aligned frames with visibility/self-similarity weighting; bank quality gate before eval; classifier; import proposal source from `study/templates/proposals/`; extend eval harness; run full eval on `2wWUKmCBr6A`, then `h2WrtkfwRl8`, then inspect failures.
- Stage 1 completed in `study/templates-v2/geometry/visibility.py`: piece-type cuboids from FEN, projection with frame-level `pose.up_sign` invariant, convex-hull silhouette rasterization, camera-depth ordering, subtraction of closer-piece unions, and crop-aligned patch masks via `compute_frame_visibility(...)` and `build_square_patch_visibility_mask(...)`.
- Tests added in `tests/test_study_templates_v2_visibility.py` cover synthetic front-pawn/back-rank occlusion and patch-grid downsampling. Acceptance-style overlays + manifest generated under `study/templates-v2/geometry/debug-stage1/`.
- `ruff`, targeted `pytest`, and targeted `mypy` pass for new files. Remaining mypy failures are pre-existing in `pipeline/physical/piece_projection.py` return types.
- Side study in `study/mlx_vlm_segmentation/` now explicitly targets **physical board stills**, not overlay frames. `python -m pipeline.cli study-vlm-segment-board` accepts either a direct physical-board image or `--annotation-id` from `data/physical/*/board_annotations.jsonl`, with `native-crop` as the default image space. It prompts `mlx-vlm` with the physical still plus a square-labeled guide image, parses per-piece polygon JSON, renders unique-color overlays, and appends a legend. Current `gemma4_local` native-crop preview on `clip_overlay_e4lGbQp4pU4_clip70_9_frame0000` produced only 3 parsed pieces under `study/mlx_vlm_segmentation/preview/`; contours remain coarse and incomplete.
