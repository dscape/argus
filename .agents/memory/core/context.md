- Failure viewer/service at `/evaluate/failures` uses `/api/evaluate/physical-failures/*`; `docker-compose.yaml` must mount `./outputs:/app/outputs`; recreate container after adding mount.
- Failure-study pipeline is episode-based and viewer auto-discovers bundles under `outputs/`; the 2026-04-16 rectified realplusmanual study (`total_episodes=14`, `selected_episodes=13`) was dominated by temporal ambiguity and supersedes older frame-sampled intuition.
- Physical annotation tooling supports split-local transient labels in `data/physical/{train,val}/transient_annotations.jsonl` via `/api/physical-{eval,train}/transient-annotation` and `/save-transient-annotation`; the existing move list is the labeling UI, hand-occluded frames are toggled on the source frame, capture is inferred from replay state, and edits autosave with serialized writes plus unchanged-payload failure suppression.
- Clip index completion/checkmark for `/annotate/physical` uses transient annotation status rather than board-frame completeness; semantic gap may remain on whether `transient_annotation_complete` exactly matches intended “all moves covered + latent states annotations,” and the user said none should currently show as complete.
- Repo audit lesson, now partly superseded by the refactor: legacy crop/oblique/joint-reader geometry paths were invalid for the active square-based physical stack; whole-board readers must be treated separately from square-based readers.
- Completed `refactor/piece_projection` reorganisation on branch `refactor/piece_projection`: `pipeline/physical/` is now organized as `board_probe/`, `two_stage/`, and `shared/`; `pipeline/physical/piece_projection.py` is the sole active physical geometry module; whole-board lane, oblique geometry lane, joint-reader bridge, legacy crop helpers, and dead scripts/tests were deleted. This invalidates/supersedes older context saying a separate `authresearch_wholeboard/` workspace still exists or that production physical runtime still routes through pre-refactor `pipeline/physical/square_classifier.py` / legacy `two_stage_board_reader.py` geometry.
- Board-probe runtime/training now use projected square-token sampling; move-model oblique/native-oblique modes were removed instead of ported; `pipeline/physical/board_probe/decoder.py` encodes the production tracker helper.
- Annotated board-probe train/eval/diagnostic paths now load original clip frames via `clip_path` + `frame_index` and pass stored board corners; rectified `row.board_path` remains baseline-only. Runtime square pooling now stays on-device, solves one board homography per sample, and fast-paths full-frame boards with direct patch-grid pooling; move-data board-neighborhood prep reuses the board-probe helper.
- Production physical default is decoder `v282` via `pipeline/physical/board_probe/decoder.py` and `weights/physical/metadata.json`; promoted production tracker eval reached `board_exact_match=0.25443786982248523`, while compatibility eval on the promoted retrained checkpoint preserved the old-lookahead baseline surface at `0.2165680473372781`.
- Two-stage remains experimental after warm-start retraining; classifier summaries were acceptable (`occupancy ≈0.8333`, `piece ≈0.7344`) even though board-level exact in `two_stage_eval_retrained.json` was poor (~0.00355).
- Verification already completed for the refactor milestone: tests, lint, typecheck, retraining/warm-start retraining gates, autoresearch cache regeneration/resweep, and local dev-tools smoke for physical routes/failure-study bundle passed.
- Relevant dev-tools route reality from later inspection: `/evaluate/physical` exists and renders; `/annotate/physical` exists; `/annotate/physical-train` appears to redirect/render under `/annotate/physical`; no separate `/annotate/physical-eval` route was found; `/evaluate/physical-runtime` and `/annotate/physical-eval` 404 under current routing, so old plan expectations may need reconciliation rather than restoration.
- `/annotate/physical/...` had an intermittent first-load 500 caused by concurrent rewrites of `data/physical/source_video_splits.json`; `pipeline/physical/shared/splits.py` now writes that manifest atomically and treats a blank file as recoverable empty state.
