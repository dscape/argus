- Failure viewer/service at `/evaluate/failures` uses `/api/evaluate/physical-failures/*`; `docker-compose.yaml` must mount `./outputs:/app/outputs`; recreate container after adding mount.
- Failure-study pipeline is episode-based and viewer auto-discovers bundles under `outputs/`; the 2026-04-16 rectified realplusmanual study (`total_episodes=14`, `selected_episodes=13`) was dominated by temporal ambiguity and supersedes older frame-sampled intuition.
- Physical annotation tooling supports split-local transient labels in `data/physical/{train,val}/transient_annotations.jsonl` via `/api/physical-{eval,train}/transient-annotation` and `/save-transient-annotation`; the existing move list is the labeling UI, hand-occluded frames are toggled on the source frame, capture is inferred from replay state, and edits autosave with serialized writes plus unchanged-payload failure suppression.
- Clip index completion/checkmark for `/annotate/physical` uses transient annotation status rather than board-frame completeness; semantic gap may remain on whether `transient_annotation_complete` exactly matches intended “all moves covered + latent states annotations,” and the user said none should currently show as complete.
- Completed `refactor/piece_projection` reorganisation on branch `refactor/piece_projection`: `pipeline/physical/` is now organized as `board_probe/`, `two_stage/`, and `shared/`; `pipeline/physical/piece_projection.py` is the sole active physical geometry module; whole-board lane, oblique geometry lane, joint-reader bridge, legacy crop helpers, and dead scripts/tests were deleted. This invalidates/supersedes older context saying a separate `authresearch_wholeboard/` workspace still exists or that production physical runtime still routes through pre-refactor geometry.
- Board-probe runtime/training now use projected square-token sampling; move-model oblique/native-oblique modes were removed instead of ported; `pipeline/physical/board_probe/decoder.py` encodes the production tracker helper.
- Native-frame contract fix: board-probe train/eval/diagnostic paths now preserve native metadata and prefer source-video frames when available, with `row.corners` resolved to full-frame native corners. This supersedes the older claim that merely switching from `row.board_path` to `clip_path + frame_index` fully aligned board-probe with the intended piece-projection contract.
- Remaining geometry leak was fixed and supersedes the earlier compaction note saying token pooling was still wrong: board-probe runtime/training/autoresearch now project piece boxes in full-frame source geometry first, then map those bboxes through the board-neighborhood crop+resize transform before patch pooling. `BoardNeighborhoodCrop` stores crop origin; datasets/real-data paths now pass `(64,4)` piece bboxes; pooling accepts either corners or precomputed piece-bbox geometry; runtime visualization and pipeline visualization use transformed full-frame boxes instead of re-projecting from resized crop corners.
- Matching tests were updated and targeted pytest passed (36 tests) for board data, real board data, board probe pooling, and piece projection transform behavior.
- The verified baseline surface after the full-frame-piecebox-to-crop mapping fix is `outputs/2026-04-18/tracker_eval_weights_best_production_native_pieceboxmapped_off.json` with `board_exact=0.03787`; an earlier report `...native_pieceboxmapped.json` was an intermediate surface and is superseded for baseline verification.
- `autoresearch/prepare.py` now rebuilds `autoresearch/cache/native_realplusmanual_board_logits.pt` from `load_annotated_board_frame_bgr(...)` plus full-frame projected piece boxes mapped into board-crop space, defaults its fixed baseline to the current piecebox-mapped report, and verifies by replaying the report’s tracker mode (`production`, `lookahead`, or `greedy`). `autoresearch/prepare.py --force` reproduces the current baseline again.
- Generated diagnostics confirmed the migration changed tokenization, not just overlays: real oblique frames showed piece-box pooling using roughly 6–20 patches per square (mean ~12.05), 124/256 patches shared by multiple squares, and adjacent-square cosine rising from 0.734 (planar) to 0.944 (piece-box); synthetic overlap was much weaker, highlighting a train/eval geometry gap.
- Post-fix retraining/eval comparison on 2026-04-17 showed corrected piece-projection board-probe metrics regressed sharply versus pre-fix promoted numbers, implying earlier metrics were inflated by old rectified-board leakage/mismatch. Two-stage stayed effectively flat; direct move-model recipe flipped from freezing to overfiring while board metrics collapsed.
- After switching eval to the native source-frame signature, rerunning the promoted checkpoint with decoder `v282` improved over the clip-frame rerun but remained far below the stale rectified/autoresearch surface; after the later piecebox-mapping fix, the verified autoresearch baseline is lower again at `board_exact=0.03787`.
- Production physical default remains decoder `v282` via `pipeline/physical/board_probe/decoder.py` and `weights/physical/metadata.json`; promoted historical runtime eval had reached `board_exact_match=0.25443786982248523`, but that is now understood as a stale/wrong-signature surface rather than the current trustworthy baseline.
- The 5-epoch post-fix board-probe retrain did not meaningfully update weights; checkpoint selection preserved the initialized state, so some apparent comparisons were different eval surfaces on the same checkpoint.
- Two-stage remains experimental after warm-start retraining; classifier summaries were acceptable while board-level exact remained poor. Inspectable CPU-only subset artifacts confirmed the two-stage path is truly crop-based on native frames, with separate occupancy vs piece geometry.
- Verification already completed for the refactor milestone and later geometry fixes: tests, lint, typecheck, retraining/warm-start retraining gates, autoresearch cache regeneration/resweep, and local dev-tools smoke for physical routes/failure-study bundle passed at the time of those changes.
- Relevant dev-tools route reality: `/evaluate/physical` exists and renders; `/annotate/physical` exists; `/annotate/physical-train` appears to redirect/render under `/annotate/physical`; no separate `/annotate/physical-eval` route was found; `/evaluate/physical-runtime` and `/annotate/physical-eval` 404 under current routing.
- `/annotate/physical/...` had an intermittent first-load 500 caused by concurrent rewrites of `data/physical/source_video_splits.json`; `pipeline/physical/shared/splits.py` now writes that manifest atomically and treats a blank file as recoverable empty state.
