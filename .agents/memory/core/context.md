- Failure viewer/service at `/evaluate/failures` uses `/api/evaluate/physical-failures/*`; `docker-compose.yaml` must mount `./outputs:/app/outputs`; recreate container after adding mount.
- Failure-study pipeline is episode-based and viewer auto-discovers bundles under `outputs/`; standard bucket labels are the slash-named taxonomy already in core. The 2026-04-16 rectified realplusmanual study (`total_episodes=14`, `selected_episodes=13`) was dominated by temporal ambiguity and supersedes older frame-sampled intuition.
- Physical annotation dev-tool now supports split-local transient clip labels persisted in `data/physical/{train,val}/transient_annotations.jsonl` via `/api/physical-{eval,train}/transient-annotation` and `/save-transient-annotation`, covering per-move start/settled/capture plus hand-occluded spans.
- `authresearch/` is a local gitignored Mac/MPS decoder workspace. `prepare.py` caches held-out logits, snapshots `train.py`, writes JSON reports, appends `results.tsv`, updates `best_train.py` on keep, and restores `train.py` from `best_train.py` on discard.
- Verified baseline remains `lookahead_w2_m10_baseline` with `board_exact=0.216568`, `non_empty_accuracy=0.863986`, `macro_f1=0.816552`, `move_recall=0.296875`, `false_change_rate=0.018111`.
- `v85` remains best overall kept reference: `min_event_separation=2`, `event_window_radius=0`, `top_board_candidates=2`, `segment_board_drop_worst_frames=1`, `max_event_proposals=17` gave `0.254438 / 0.884347 / 0.856334 / 0.500000 / 0.018111`.
- Stable sibling tradeoff above `v85`: `v89`/`v93`-`v96`/`v109`-`v116`/`v132` (`props18`) matched `board_exact=0.254438` with better `non_empty_accuracy=0.898455` and `macro_f1=0.860396`, same recall, but worse `static_false_change_rate=0.019405`; nearby threshold/weight tweaks were no-ops.
- `v120` invalidates the earlier belief that all improved-family `props16` variants are bad: `props16 + top_board_candidates=3` reached `0.254438 / 0.884089 / 0.855756 / 0.500000 / 0.016818`. `v121`-`v144` showed this branch is mostly saturated except for hybrid gain gating.
- Latest gain-gate lesson in old `props16 + boardcands3`: `min_total_score_gain` is inactive if per-move gain stays high (`v146`), but relaxing `HYBRID_CONFIG["min_score_gain_per_added_move"]` triggers a cliff. Safe/harmful bracket supersedes older wording: safe `v207=91.7342681884765700009443102` / `v205=91.7342681884765705213613530` vs harmful `v206=91.7342681884765694805272674` / `v204=91.73426818847656843969318185`; no third regime emerged.
- `v210` invalidates the assumption that the restored `v85` neighborhood is fully inert: `beam_size=6` is active and creates a tighter-search regime (`0.254438 / 0.893597 / 0.864840 / 0.484375 / 0.018111`), while `beam7` is no-op and `beam<=2` degrades.
- `v221` supersedes older low-false-change focus as the main active rescued branch: `beam6 + props16 + boardcands2` reached `0.254438 / 0.893339 / 0.864286 / 0.484375 / 0.016818`; nearby `boardcands3`, `top12`, `detect_peak_threshold=0.05`, and `min_total_score_gain=125` were no-ops (`v222`, `v227`, `v232`, `v233`).
- `v253` supersedes `v225` as the strongest low-false-change props15 lane: `beam3 + props15 + boardcands2` reached `0.254438 / 0.876337 / 0.830116 / 0.484375 / 0.015524`; `boardcands3`, `top12`, `detect5`, `total125`, `event_window_radius=1`, `top9`, `top8`, `top6`, and `permove118.75` were no-ops (`v254`, `v256`, `v257`, `v259`, `v262`, `v263`, `v267`, `v268`, `v266`), `props14` collapsed (`v258`), `secondarysep1` was slightly harmful (`v261`), `beam4`/`beam5` fall back to weaker props15 behavior (`v255`, `v264`), and lowering per-move gain to `112.5` or `100` was catastrophic (`v265`, `v260`).
- Newer `props16 + top2` mapping supersedes older focus on only `v221`/`v253`: on rescued `beam6 + props16`, `top9`/`8`/`6`/`4`/`3` all snap to `v221` (`v269`-`v274` except `v273`), while `top2`/`top1` create a distinct higher-recall regime `0.254438 / 0.889670 / 0.859107 / 0.500000 / 0.016818` (`v273`, `v275`); this regime persists at `beam5` and `beam7` (`v279`, `v281`) but `beam4`/`beam3` snap back (`v280`, `v278`).
- `v282` supersedes older core focus on beam6/beam3 as the only active low-false-change branches: `beam8 + props16 + top2` opened a distinct branch at `0.254438 / 0.847191 / 0.844308 / 0.484375 / 0.015524`; `top1`, `total125`, and `permove112.5` were exact no-ops (`v284`, `v290`, `v291`), `top3` was worse (`v283`), `boardcands3` shifts toward the `v120`-style tradeoff (`v285`), `boardcands1` is harmful (`v289`), `props15` is worse (`v286`), and `props17`/`props17+boardcands3` are worse/no-op within their own weaker family (`v287`, `v288`).
- Compaction-note update invalidates older wording that `v292` weakened metrics: `v292 = beam8 + props16 + top2 + permove100` was an exact no-op vs `v282`, and later mapping showed the beam8 per-move cliff is safe through `93.75` (`v296`) but harmful at `87.5` and below (`v295`, `v294`, `v293`).
- New compaction-note mapping supersedes older beam-family summary: `beam5`, `beam7`, and `beam8` all share the same per-move cliff interval on active `props16 + top2` branches (safe at `93.75`, harmful at `87.5`); `beam5` and `beam7` also share the same move-menu cliff where `top1/top2` are identical active higher-recall regimes and `top3+` snap back.
- New compaction-note mapping supersedes older board-width summary: `beam7 + props16 + top2 + boardcands3` is inert (`v298`), while `beam5 + boardcands3/4` snap back to the `v221`-style regime (`v313`, `v314`), and `beam7/8 + boardcands4` are worse (`v299`, `v297`).
- Relevant decoder logic: in `pipeline/shared/board_tracking.py`, `_proposal_centers(...)` uses `secondary_min_event_separation` / `secondary_peak_ratio`, `_event_window(...)` uses `event_window_radius`, segmental acceptance gates on `move_increment < stay_increment + move_score_margin`, and `segment_board_drop_worst_frames` drops worst frame scores before summing segment board evidence.
- Separate `authresearch_wholeboard/` workspace exists for native whole-board direct reader experiments with autoregressive eval and optional previous-board context.
- Whole-board results so far are dominated by static-copy behavior: many experiments tie exactly at `tracker_board_exact=0.061538`, `tracker_non_empty_accuracy=0.835512`, `tracker_macro_f1=0.768594`, `move_recall=0.000000`, `false_change_rate=0.000000`.
- Whole-board lane pivoted from DINOv2 to SigLIP2 because DINOv2 variants kept tying on physical metrics.
- In whole-board SigLIP2/input256 runs, removing previous-board conditioning (`PREVIOUS_BOARD_CONDITIONING = "none"`) was harmful, so previous-board context matters in that local family.
- Whole-board `dropout=0.3` on the SigLIP2/input256/prevboard-gated/sidetomove setup was a no-op on physical tracker metrics.
- Later compaction-note inspection supersedes the earlier unrun-prepared state: `nativecrop_direct_board_reader_siglip2_prevboard_gated_input256_sidetomove_linearhead_wd1e3_smoke1` was observed in `run.log` and underperformed badly on chessred validation, while `best_train.py` and `train.py` were aligned to that same experiment surface at inspection time.
- Whole-board environment warning remains non-fatal but persistent: duplicate `AVFFrameReceiver` / `AVFAudioReceiver` implementations from `cv2` and `av` may cause spurious casting failures/crashes.
