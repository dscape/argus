- Failure viewer/service at `/evaluate/failures` uses `/api/evaluate/physical-failures/*`; `docker-compose.yaml` must mount `./outputs:/app/outputs`; recreate container after adding mount.
- Failure-study pipeline is episode-based and viewer auto-discovers bundles under `outputs/`; the 2026-04-16 rectified realplusmanual study (`total_episodes=14`, `selected_episodes=13`) was dominated by temporal ambiguity and supersedes older frame-sampled intuition.
- Physical annotation tooling now supports split-local transient labels in `data/physical/{train,val}/transient_annotations.jsonl` via `/api/physical-{eval,train}/transient-annotation` and `/save-transient-annotation`. Supersedes older wording about a separate panel/manual capture labeling: the existing move list is the labeling UI, per-move touch start/end are set there, hand-occluded frames are toggled directly on the source frame, capture is inferred from replay state, and edits autosave with serialized writes plus unchanged-payload failure suppression.
- Clip index completion/checkmark for `/annotate/physical` now uses transient annotation status rather than board-frame completeness; possible semantic gap remains whether `transient_annotation_complete` exactly matches the user’s intended “all moves covered + latent states annotations,” and the user stated none should currently show as complete.
- `authresearch/` is a local gitignored Mac/MPS decoder workspace. `prepare.py` caches held-out logits, snapshots `train.py`, writes JSON reports, appends `results.tsv`, updates `best_train.py` on keep, and restores `train.py` from `best_train.py` on discard.
- Verified baseline remains `lookahead_w2_m10_baseline` with `board_exact=0.216568`, `non_empty_accuracy=0.863986`, `macro_f1=0.816552`, `move_recall=0.296875`, `false_change_rate=0.018111`.
- Best/reference decoder families: `v85` best overall kept; `v89`/`props18` same board_exact with better non-empty/macro-F1 but worse static false-change; `v120` invalidates the claim that all improved-family `props16` variants are bad; `v210` invalidates the claim restored `v85` is inert by showing active `beam6`; `v221` is the main rescued low-false-change `beam6 + props16 + boardcands2` branch; `v253` is strongest low-false-change `props15`; `v273` is the higher-recall `beam6/5/7 + props16 + top2/top1` regime; `v282` opened distinct `beam8 + props16 + top2` low-false-change branch.
- Decoder mapping lessons from compaction notes: proposal budgets above 18 remain unhelpful; `v85`/`v89` neighborhoods are saturated; old `v120` gain-gate branch is saturated except for a cliff; `beam3 props15` has a sharp harmful per-move cliff; `beam5/7/8 props16 + top2` share the same per-move cliff (safe at `93.75`, harmful at `87.5`), with `beam5/7` also sharing move-menu cliffs where `top1/top2` are identical active regimes and `top3+` snap back; board-width mapping shows `beam7 + boardcands3` inert, `beam5 + boardcands3/4` snap back to `v221`-style, and `beam7/8 + boardcands4` are worse. This supersedes older narrower beam-family summaries.
- Relevant decoder logic: in `pipeline/shared/board_tracking.py`, `_proposal_centers(...)` uses `secondary_min_event_separation` / `secondary_peak_ratio`, `_event_window(...)` uses `event_window_radius`, segmental acceptance gates on `move_increment < stay_increment + move_score_margin`, and `segment_board_drop_worst_frames` drops worst frame scores before summing segment board evidence.
- Separate `authresearch_wholeboard/` workspace exists for native whole-board direct reader experiments with autoregressive eval and optional previous-board context.
- Whole-board results so far are dominated by static-copy behavior; many runs tie at `tracker_board_exact=0.061538`, `tracker_non_empty_accuracy=0.835512`, `tracker_macro_f1=0.768594`, `move_recall=0.000000`, `false_change_rate=0.000000`.
- Whole-board lane pivoted from DINOv2 to SigLIP2 because DINOv2 variants kept tying on physical metrics. In local SigLIP2/input256 runs, removing previous-board conditioning was harmful, dropout `0.3` was a no-op, and later inspection supersedes the earlier unrun-prepared state: `nativecrop_direct_board_reader_siglip2_prevboard_gated_input256_sidetomove_linearhead_wd1e3_smoke1` was observed in `run.log` and underperformed badly on chessred validation while `best_train.py` and `train.py` were aligned to that surface at inspection time.
- Whole-board environment warning remains non-fatal but persistent: duplicate `AVFFrameReceiver` / `AVFAudioReceiver` implementations from `cv2` and `av` may cause spurious casting failures/crashes.
