- Current objective: improve physical board tracking on the rectified realplusmanual split by addressing dominant temporal move-execution ambiguity.
- Cheap decoder-only work in `authresearch/` should stay centered on active hybrid `props16` subbranches; the local focus now includes systematic neighborhood mapping across `beam5/7/8` rather than only the earlier `beam8 + props16 + top2` branch.
- Main decoder tradeoff references now are: `v85` (best overall kept profile), stable `v89`/`props18`, `v221` (beam6 props16 rescued branch / snapped-back family), `v273` (beam6/5/7 props16 top2 higher-recall regime), `v253` (best props15 low-false-change branch), and `v282` (beam8 props16 top2 low-false-change branch).
- Immediate authresearch state supersedes older focus that a next experiment was already left prepared: `v314` (`beam5 + props16 + top2 + boardcands4`) was run, discarded, and `run.log` showed `restored_train: true`; no later prepared-but-unrun decoder experiment remains from the compaction note.
- Next turn in `authresearch/` should first inspect `results.tsv`, `run.log`, `best_train.py`, and `train.py` to confirm post-`v314` restored state, then leave exactly one prepared experiment in `authresearch/train.py` without running it.
- Working interpretation: proposal budgets above `18` remain unhelpful; `v85`/`v89` neighborhoods are saturated; old `v120` gain-gate branch is saturated except for a cliff; `beam3 props15` is saturated with a sharp harmful per-move cliff; `beam6 props16` is mostly active only at the exact `top2/top1` cliff; `beam5/7/8 props16` now have largely mapped move-width, board-width, and per-move cliffs.
- Since repeated midpoint bisection across `beam5/7/8` found the same safe `93.75` vs harmful `87.5` split, avoid more per-move-gate bisection; prefer one tiny nearby shape probe on a still-unmapped local lever.
- Keep the failure-study viewer and episode-based sampler as the standard diagnosis loop.
- Parallel focus in `authresearch_wholeboard/`: stay on native whole-board reader experiments and confirm what the next prepared experiment should be after the failed SigLIP2 linear-head/WD smoke run.
- Immediate whole-board next turn should inspect `authresearch_wholeboard/results.tsv`, `run.log`, `best_train.py`, and `train.py`, then leave exactly one prepared experiment in `authresearch_wholeboard/train.py` without running it; keep changes tiny and likely restore toward a safer nearby baseline before the next knob test.
