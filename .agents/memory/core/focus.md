- Current objective: improve physical board tracking on the rectified realplusmanual split by addressing dominant temporal move-execution ambiguity.
- Decoder focus remains cheap, decoder-only `authresearch/` work on active hybrid `props16` subbranches, especially a still-unmapped tiny shape probe rather than more per-move midpoint bisection.
- Immediate `authresearch/` next step: inspect `results.tsv`, `run.log`, `best_train.py`, and `train.py` to confirm post-`v314` restored state, then leave exactly one prepared experiment in `authresearch/train.py` without running it.
- Main decoder tradeoff references: `v85`, stable `v89`/`props18`, `v221`, `v273`, `v253`, and `v282`.
- Parallel tooling focus: verify the physical transient-annotation UX/end-to-end behavior on `/annotate/physical` and a clip page, especially that no clips currently show complete, move rows remain minimal, per-marker `×` works, frame occlusion toggling is usable, and autosave behavior feels correct.
- If verification shows mismatch, adjust completion logic in `dev-tools/api/services/annotate/physical_eval_service.py` / related train service so index-page checkmarks match the intended meaning of complete transient/latent coverage.
- Parallel repo-audit/crop focus: keep the current architecture straight while planning any cleanup—board-probe runtime is active, projected crops are only wired into the two-stage square-classifier dataset path, and whole-board readers are a separate lane.
- Parallel whole-board focus: inspect `authresearch_wholeboard/results.tsv`, `run.log`, `best_train.py`, and `train.py`, then leave exactly one tiny prepared experiment in `authresearch_wholeboard/train.py` without running it; likely restore toward a safer nearby baseline before another knob test.
