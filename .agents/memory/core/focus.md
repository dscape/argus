- Primary objective: finish `/evaluate/physical/<session>` runtime-inspection UX so the displayed image is trustworthy for geometry inspection and matches the piece-projection contract.
- Immediate next step: debug why the client overlay was reportedly not visible after reload despite backend payloads/tests passing; inspect live session/API payloads for `image_render_mode=source_frame_raw`, `geometry_space=source_frame_normalized`, populated geometry arrays, and verify the overlay component mounts and is visible in-browser.
- Likely direction: display a board-focused crop with padding around projected board/piece geometry while preserving client-side overlays aligned to that displayed image.
- Parallel objective: continue corrected-native autoresearch only through one small decoder-only edit in `autoresearch/train.py`.
- Current autoresearch focus supersedes older `v795`/`v819`/`v820`/`v841`/`v865` wait states: the active local search is the compact `props8 + minsep8` lower-noise shelf in the `beam3 + LOOKAHEAD_WINDOW=3 + top_board_candidates=2 + secsep1` family, now narrowed to the machine-precision lower edge of `min_score_gain_per_added_move`.
- Next autoresearch loop: inspect `autoresearch/results.tsv`, `autoresearch/run.log`, `autoresearch/best_train.py`, and `train.py`; confirm controller execution/results for `v884`; compare against the stable `v805/v806/v808/v811/v812/v816/v817/v818` anchor and the degraded `v820/v839..v881` regime; then prepare exactly one adjacent cheap follow-up only if needed.
- If `v884` is flat, resume from the true executed bracket `v881=142.33938090006504` harmful vs `v883=142.33938090006524` flat; the next informative midpoint is about `142.33938090006513`.
- Preferred current autoresearch probe awaiting controller is `v884`: keep `LOOKAHEAD_WINDOW=3`, `LOOKAHEAD_MARGIN=9.935836791992188`, `beam_size=3`, `top_move_candidates=4`, `top_board_candidates=2`, `min_event_separation=8`, `secondary_min_event_separation=1`, `secondary_peak_ratio=0.95`, `segment_board_drop_worst_frames=1`, `event_window_radius=0`, `max_event_proposals=8`, `min_total_score_gain=150.0`, and test only `min_score_gain_per_added_move=142.33938090006532`.
- Parallel objective: plan the requested side-by-side Argus-vs-Chesscog occupancy/piece-classification comparison on Argus data.
