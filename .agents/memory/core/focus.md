- Primary objective: finish `/evaluate/physical/<session>` runtime-inspection UX so the displayed image is trustworthy for geometry inspection and matches the piece-projection contract.
- Immediate next step: debug why the client overlay was reportedly not visible after reload despite backend payloads/tests passing; inspect live session/API payloads for `image_render_mode=source_frame_raw`, `geometry_space=source_frame_normalized`, populated geometry arrays, and verify the overlay component mounts and is visible in-browser.
- Likely direction: display a board-focused crop with padding around projected board/piece geometry while preserving client-side overlays aligned to that displayed image.
- Parallel objective: continue corrected-native autoresearch only through one small decoder-only edit in `autoresearch/train.py`.
- Current autoresearch focus supersedes older midpoint-cliff and generic anchor probing: machine-precision margin search is finished, threshold sweeps were flat, `props7` local breadth/spacing has been heavily saturated, and the active local search now includes the adjacent compact `props8` sibling in the same `beam3 + LOOKAHEAD_WINDOW=3 + top_board_candidates=2 + minsep7 + secsep1` family.
- Next autoresearch loop: inspect `autoresearch/results.tsv`, `autoresearch/run.log`, `autoresearch/best_train.py`, and `train.py`; confirm whether controller executed `v795`; compare against `v792`/`v794`; then prepare exactly one adjacent cheap follow-up from the `props8` shelf, re-encoding it from the restored `v545` baseline if needed.
- Preferred next autoresearch direction after `v795`: keep `LOOKAHEAD_WINDOW=3`, `LOOKAHEAD_MARGIN=9.935836791992188`, `beam_size=3`, `top_board_candidates=2`, `min_event_separation=7`, `secondary_min_event_separation=1`, and `max_event_proposals=8`, and test one remaining nearby breadth/spacing knob not already proven flat/worse.
- Parallel objective: plan the requested side-by-side Argus-vs-Chesscog occupancy/piece-classification comparison on Argus data.
