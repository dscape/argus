- Primary objective is to finish the `/evaluate/physical/<session>` runtime-inspection UX so the displayed image is visually trustworthy for geometry inspection and matches the piece-projection contract.
- Immediate next step: debug why the new client overlay was reportedly not visible after reload despite backend payloads/tests passing; inspect live session/API payloads for `image_render_mode=source_frame_raw`, `geometry_space=source_frame_normalized`, and populated geometry arrays, then verify the overlay component mounts and is visible in-browser.
- Incorporate the latest user preference from the compaction note: the runtime display should likely be a board-focused crop with padding around the projected board/piece geometry, while preserving client-side overlays aligned to that displayed image.
- In parallel, continue the corrected-native autoresearch path: decide whether to archive/reset stale `autoresearch/results.tsv` / `autoresearch/best_train.py`, then run a fresh decoder resweep on the rebuilt native-frame cache and compare results against `outputs/2026-04-18/tracker_eval_weights_best_production_native.json` and the stale rectified/autoresearch surface.
- Secondary cleanup remains route reconciliation/documentation around missing `/evaluate/physical-runtime` and `/annotate/physical-eval`, plus eventual final browser verification of `/evaluate/physical/<session>` once the overlay/crop behavior is confirmed.
