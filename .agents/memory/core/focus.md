- Primary objective is still to finish the `/evaluate/physical/<session>` runtime-inspection UX so the displayed image is visually trustworthy for geometry inspection and matches the piece-projection contract.
- Immediate next step: debug why the new client overlay was reportedly not visible after reload despite backend payloads/tests passing; inspect live session/API payloads for `image_render_mode=source_frame_raw`, `geometry_space=source_frame_normalized`, populated geometry arrays, and verify the overlay component mounts and is visible in-browser.
- Incorporate the latest user preference: runtime inspection should likely use a board-focused crop with padding around the projected board/piece geometry while preserving client-side overlays aligned to that displayed image.
- In parallel, continue the corrected-native autoresearch path only after deciding how to handle stale history (`autoresearch/results.tsv`, `autoresearch/best_train.py`) and then run a fresh decoder resweep against the rebuilt native-frame cache on the current piecebox-mapped baseline surface.
- Use source-of-truth visual artifacts when discussing geometry, including `outputs/piece_projection_review2/*` and the new c8 debug attachments, but do not treat old wrong patch-mask visuals as authoritative.
