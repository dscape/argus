- Inspect the latest `authresearch/results.tsv`, `authresearch/run.log`, `authresearch/best_train.py`, and current `authresearch/train.py` to confirm the post-`v314` restored/prepared file state before any further preparation; do not run manually from memory maintenance.
- After the next `authresearch` run, compare it first against the branch-local reference for whatever branch it follows (`v282`, `v273`, `v253`, or `v221`), then against `v120`, `v85`, and stable `v89`/`props18`.
- Unresolved decoder search decision: which single nearby lever to try next now that `beam5/7/8` props16 families have mostly mapped move-width, board-width, and per-move cliffs; prefer a small shape probe rather than more per-move tuning.
- Preserve the lesson that continuing old per-move-gate bisection is low-yield: latest confirmed bracket supersedes the older `v178`/`v182` one and ended at safe `v207`/`v205` vs harmful `v206`/`v204`, with only two regimes and no improvement.
- Confirm/manual-complete final bucket tagging for all selected episodes in `manual_buckets.csv` if any rows remain heuristic rather than reviewed.
- Open question: whether `max_per_video=5` should remain for future studies when total episodes are low (`14` total, `13` selected here).
- Open tooling question: `record_successful_run` / keep logic appears heavily driven by `board_exact`, so tied-but-better secondary-metric variants may still be discarded or over-kept.
- Verify current physical transient-annotation UI in browser at `/annotate/physical` and a clip page; confirm no current checkmarks, desired minimal move-row UX, per-marker clear behavior, usable frame-occlusion toggling, and autosave semantics.
- If needed, tighten the meaning of `transient_annotation_complete` so clip-index completion matches the user’s intended “all moves covered + latent states annotations.”
- Optional cleanup after UX verification: remove any now-unused fields/logic in `usePhysicalTransientAnnotations.ts` and `PhysicalAnnotationPage.tsx`.
- Note blocked tooling issue: `make lint` was failing for unrelated `tests/pipeline/test_piece_projection.py` import-order/unused-import errors; final `make test` state after the last UI change was not clearly shown.
- Whole-board next-step follow-up: verify whether `nativecrop_direct_board_reader_siglip2_prevboard_gated_input256_sidetomove_linearhead_wd1e3_smoke1` completed and how it compares against `...linearhead_smoke1`, then prepare exactly one new tiny whole-board experiment in `authresearch_wholeboard/train.py` without running it.
- Whole-board unresolved decision: after the poor `linearhead_wd1e3` result, choose the safest next narrow change in the SigLIP2/input256/prevboard family, likely by restoring toward a safer nearby baseline first.
- Whole-board open question: repeated exact ties may mean the lane is stuck in a static-copy regime; if another narrow SigLIP2 variant no-ops, decide which minimal anti-copy lever to try next.
- Whole-board broader follow-ups remain: finish/verify scaffold beyond `prepare.py`, decide controller/runtime budget and ranking details, run an initial stable baseline/smoke sequence, and decide whether non-physical data should also get analogous board-neighborhood cropping for consistency.
- Whole-board open question: once the baseline is stable, decide whether to test previous-board conditioning (`add` or `gated`) and previous side-to-move conditioning more broadly, given the autoregressive eval path and prepared prior-board context.
- Environment warning remains noted but unblocked: duplicate `AVFFrameReceiver` / `AVFAudioReceiver` implementations from `cv2` and `av` may cause spurious casting failures/crashes.
