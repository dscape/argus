defaults:
  - model: argus_base
  - data: synthetic
  - training: phase1_detection
  - evaluate: default
  - _self_

seed: 42
init_checkpoint: null
output_dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
